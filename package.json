{
    "name": "playwright-test",
    "version": "1.0.0",
    "description": "Test skeleton for playwright element manipulation and screenshotting",
    "scripts": {
        "test": "npx playwright test --config=playwright.config.ts"
    },
    "type":"module",
    "devDependencies": {
        "@playwright/test": "^1.35.1",
        "@types/node": "^22.2.0"
    }
}
